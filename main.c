#include "main.h"


/**
 * main - reads commands and executes
 * argc Argument count.
 * param argv Argument vector.
 * Return: return status code
 */
int main(void)
{
_loop();
return (EXIT_SUCCESS);
}

